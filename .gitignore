# dependencies / build output
node_modules/
app/node_modules/
app/dist/

# secrets — never commit
.env
*.env
scripts/.env
!scripts/.env.example

# live business data (real employee names + financials) — generated by the snapshot
app/public/assets/data/real.json

# third-party assets that can't be redistributed (download LimeZu free yourself — see README)
app/public/assets/limezu/

# temp / scratch
*.tmp
*.log

# OS
.DS_Store
Thumbs.db

# посторонние проекты в этой папке (не часть biz-game)
lottie-player/
lottie-test/
